# Plan

- Compartilhar somente contratos HTTP/JSON.
- Centralizar chamadas em `web/src/api.ts`.
- Configurar API por variável de ambiente.
- Manter mock isolado em `mock-api/`.
- Documentar qualquer divergência entre mock e contrato oficial.
