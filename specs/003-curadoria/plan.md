# Plan

- Fila carregada por GET da API.
- Decisão enviada por `POST /api/versoes/{id}/decisao`.
- Iframe de preview com sandbox quando `preview_url` existir.
- Interface seguindo a mesma linguagem visual do G3.
